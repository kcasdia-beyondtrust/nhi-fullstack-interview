# Pin the public registry. Without this, npm inherits whatever registry the
# machine that generated the lockfile was configured for -- which bakes an
# internal mirror into package-lock.json and 401s anywhere else.
registry=https://registry.npmjs.org/
