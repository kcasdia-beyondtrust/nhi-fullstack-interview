# Loose lower bounds on purpose: this scaffold runs on whatever Python the
# environment has (3.11+), and pinning exact patch versions tends to break
# that more often than it helps.
Flask>=3.1,<4
flask-cors>=6.0,<7
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.2,<4
pytest>=8.0
